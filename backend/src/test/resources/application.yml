spring:
  datasource:
    url: jdbc:h2:mem:project_manager_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  sql:
    init:
      mode: never

app:
  jwt:
    secret: test-secret-key-test-secret-key-test-secret-key-test-secret-key
    access-token-expiration-seconds: 3600
